variables:
  MANIFEST: "git@g.a-bug.org:canmv/k230/manifest.git"
  REPO_URL: "https://gitee.com/mamh-mixed/git-repo"
  STUB_BOARD: "k230_canmv_01studio"
  CANMV_VSCODE_EXTENSION_PUBLIC_URL: "https://download.kendryte.com/developer/tools/canmv_vscode_extension"

default:
  image: ai.b-bug.org:5000/k230_sdk:latest
  tags:
    - canmv_k230

stages:
  - build
  - generate

.generate_pipeline_version: &generate_pipeline_version
  - echo "----------generate pipeline version----------"
  - echo "generate version with tag or commit id"
  - |
      version=$(date "+%Y%m%d-%H%M%S") || exit 1;
      echo "ver is ${version}";
  - echo ${version} > $CI_PROJECT_DIR/version || exit 1
  - cat $CI_PROJECT_DIR/version || exit 1

before_script:
  - echo '----------Build ENV Prepare----------'
  - echo 'Add SSH KEY for Multiple repo clone in Makefile'
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - cp "$DNLD_SRV_KEY" ~/.ssh/id_rsa
  - chmod 600 ~/.ssh/id_rsa
  - eval "$(ssh-agent -s)"
  - ssh-add ~/.ssh/id_rsa
  - |
      if [ -n "${MY_SSH_PRIVATE_KEY:-}" ] && [ -f "${MY_SSH_PRIVATE_KEY}" ]; then
        cp "${MY_SSH_PRIVATE_KEY}" ~/.ssh/id_repo_rsa
        chmod 600 ~/.ssh/id_repo_rsa
        ssh-add ~/.ssh/id_repo_rsa
      fi
  - |
      if [ -n "${SSH_PRIVATE_KEY:-}" ]; then
        printf '%s\n' "${SSH_PRIVATE_KEY}" | tr -d '\r' | ssh-add -
      fi
  - '[[ -f /.dockerenv ]] && echo -e "Host *\n    StrictHostKeyChecking no\n" > ~/.ssh/config'
  - echo $SHELL
  - echo "config ssh timeout"
  - echo "    ServerAliveInterval 30" >> ~/.ssh/config
  - echo "    ServerAliveCountMax 60" >> ~/.ssh/config
  - echo "    TCPKeepAlive yes" >> ~/.ssh/config
  - cat ~/.ssh/config
  - whoami
  - uptime
  - pwd
  - ls -alht
  - uname -a
  - ls -alht $CI_PROJECT_DIR
  - cat /etc/issue
  - echo $CI_PROJECT_DIR
  - echo $CI_PROJECT_NAME
  - echo $CI_PIPELINE_SOURCE
  - echo '----------Set git config----------'
  - echo "${GITLAB_USER_EMAIL}"
  - echo "${GITLAB_USER_NAME}"
  - git config --global user.email "${GITLAB_USER_EMAIL}"
  - git config --global user.name "${GITLAB_USER_NAME}"
  - git config --global --add safe.directory "$CI_PROJECT_DIR"

# build setup for all jobs
build_setup:
  stage: .pre
  rules:
    # MR job
    # - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    # manual job
    - if: $CI_PIPELINE_SOURCE == "web"
    # daily build
    - if: $CI_PIPELINE_SOURCE == "schedule"
  timeout: 5m
  script:
    - echo "build env setup"
    - echo "generate version file"
    - *generate_pipeline_version
    - echo "version=${version}" > setup.env
  artifacts:
    reports:
      dotenv: setup.env

build-image-and-stubs:
  stage: build
  timeout: 3 hours
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "canmv_k230" || $CI_COMMIT_BRANCH == "test_ci")'
  script:
    - echo '---------- Check changed files ----------'
    - |
        changed_files=""
        if [ "${CI_COMMIT_BEFORE_SHA}" != "0000000000000000000000000000000000000000" ]; then
          git fetch origin "${CI_COMMIT_BEFORE_SHA}" --depth=1 || true
          changed_files="$(git diff --name-only "${CI_COMMIT_BEFORE_SHA}" "${CI_COMMIT_SHA}" || true)"
        fi
        if [ -z "${changed_files}" ]; then
          changed_files="$(git diff-tree --root --no-commit-id --name-only -r "${CI_COMMIT_SHA}" || true)"
        fi
        echo "Changed files:"
        printf '%s\n' "${changed_files}"
        non_firmware_changes="$(printf '%s\n' "${changed_files}" | grep -v -E '^resources/(examples|models)(/|$)' | grep -v -E '^[[:space:]]*$' || true)"
        if [ -n "${changed_files}" ] && [ -z "${non_firmware_changes}" ]; then
          echo "Only VS Code examples/models resources changed; skip image build and stub generation."
          exit 0
        fi

    - echo '---------- Download Repo Tool ----------'
    - mkdir -p ${HOME}/bin
    - wget -q https://ai.b-bug.org/k230/test_resources/ci/tools/repo -O ${HOME}/bin/repo
    - chmod +x ${HOME}/bin/repo
    - export PATH="${HOME}/bin:${PATH}"
    - repo --version

    - echo '---------- Install Python Dependencies ----------'
    - pip3 install --quiet jsonschema jinja2

    - echo '---------- Init and Sync Repo ----------'
    - mkdir -p ${HOME}/canmv_k230 && cd ${HOME}/canmv_k230
    - time repo init -u ${MANIFEST} --repo-url=${REPO_URL}
    - time repo sync -j$(nproc)

    - echo "---------- Checkout canmv ${CI_COMMIT_BRANCH}/${CI_COMMIT_SHA} ----------"
    - pushd ${HOME}/canmv_k230/src/canmv > /dev/null || exit 1
    - |
        CANMV_REMOTE="$(git remote | sed -n '1p')"
        if [ -z "${CANMV_REMOTE}" ]; then
          echo "No git remote found in src/canmv"
          exit 1
        fi
        git fetch "${CANMV_REMOTE}" "${CI_COMMIT_BRANCH}"
        git checkout "${CI_COMMIT_SHA}"
        git branch --show-current || true
        git rev-parse HEAD
    - popd > /dev/null || exit 1

    - echo '---------- Download Toolchain ----------'
    - cd ${HOME}/canmv_k230
    - export NATIVE_BUILD=1
    - mkdir -p ~/.kendryte
    - make dl_toolchain TOOLCHAIN=rtsmart

    - echo '---------- Build Image ----------'
    - export SKIP_TOOLCHAIN_CHECK=1
    - make ${STUB_BOARD}_defconfig
    - time make -j$(nproc)

    - echo '---------- Generate CanMV K230 Stubs ----------'
    - mkdir -p ${CI_PROJECT_DIR}/generated
    - export STUB_DIR="${CI_PROJECT_DIR}/generated/${CI_COMMIT_SHA}"
    - export VSCODE_ASSET_DIR="${CI_PROJECT_DIR}/generated/canmv_vscode_extension"
    - python3 ${HOME}/canmv_k230/src/canmv/tools/gen_canmv_stubs.py --build-dir ${HOME}/canmv_k230/output/${STUB_BOARD}_defconfig/canmv --out "${STUB_DIR}"
    - python3 -m compileall -q "${STUB_DIR}"
    - python3 ${HOME}/canmv_k230/src/canmv/tools/package_canmv_vscode_examples.py ${HOME}/canmv_k230/src/canmv/resources --out "${VSCODE_ASSET_DIR}" --commit "${CI_COMMIT_SHA}"
    - |
        EXAMPLES_ID="$(cat "${VSCODE_ASSET_DIR}/examples/latest")"
        echo "Current examples id: ${EXAMPLES_ID}"
        python3 ${HOME}/canmv_k230/src/canmv/tools/package_canmv_vscode_stubs.py \
          --stubs-dir "${STUB_DIR}" \
          --out "${VSCODE_ASSET_DIR}" \
          --firmware-commit "${CI_COMMIT_SHA}" \
          --board "${STUB_BOARD}" \
          --build-config "${STUB_BOARD}_defconfig" \
          --examples-id "${EXAMPLES_ID}"
    - ls -alh ${CI_PROJECT_DIR}/generated

    - echo '---------- Upload CanMV K230 Stubs ----------'
    - ssh -p "$DNLD_SRV_PORT" "$DNLD_SRV" "mkdir -p '$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/stubs' '$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/firmware/${CI_COMMIT_SHA}' '$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/examples'"
    - |
        if ssh -p "$DNLD_SRV_PORT" "$DNLD_SRV" "test -f '$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/examples/${EXAMPLES_ID}.zip'"; then
          echo "Examples package already exists: ${EXAMPLES_ID}"
        else
          scp -P $DNLD_SRV_PORT -C "${VSCODE_ASSET_DIR}/examples/${EXAMPLES_ID}.zip" "${VSCODE_ASSET_DIR}/examples/${EXAMPLES_ID}.json" "${VSCODE_ASSET_DIR}/examples/latest" $DNLD_SRV:$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/examples/
        fi
    - scp -P $DNLD_SRV_PORT -C "${VSCODE_ASSET_DIR}"/stubs/*.zip "${VSCODE_ASSET_DIR}"/stubs/*.json "${VSCODE_ASSET_DIR}"/stubs/latest $DNLD_SRV:$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/stubs/
    - scp -P $DNLD_SRV_PORT -C "${VSCODE_ASSET_DIR}/firmware/${CI_COMMIT_SHA}/manifest.json" $DNLD_SRV:$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/firmware/${CI_COMMIT_SHA}/
    - scp -P $DNLD_SRV_PORT -C "${VSCODE_ASSET_DIR}/firmware/latest" $DNLD_SRV:$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/firmware/latest
  artifacts:
    when: on_success
    expire_in: 1 week
    paths:
      - generated/canmv_vscode_extension/stubs/*
      - generated/canmv_vscode_extension/firmware/latest
      - generated/canmv_vscode_extension/firmware/**/*
      - generated/canmv_vscode_extension/examples/latest

generate-resources:
  stage: generate
  script:
    - python3 tools/package_canmv_vscode_examples.py resources --out generated/canmv_vscode_extension --commit "${CI_COMMIT_SHA}"
    - ls -ald generated/*
    - export EXAMPLES_ID="$(cat generated/canmv_vscode_extension/examples/latest)"
    - ssh -p "$DNLD_SRV_PORT" "$DNLD_SRV" "mkdir -p '$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/examples'"
    - scp -P $DNLD_SRV_PORT -C "generated/canmv_vscode_extension/examples/${EXAMPLES_ID}.zip" "generated/canmv_vscode_extension/examples/${EXAMPLES_ID}.json" generated/canmv_vscode_extension/examples/latest $DNLD_SRV:$DNLD_SRV_TOOLS_CANMV_IDE_K230_PATH/examples/
  artifacts:
    when: on_success
    expire_in: 1 week
    paths:
      - generated/canmv_vscode_extension/examples/*

  rules:
  - if: '$CI_COMMIT_BRANCH == "canmv_k230" || $CI_COMMIT_BRANCH == "test_ci"'
    changes:
        - resources/examples/**/*
        - resources/models/**/*
